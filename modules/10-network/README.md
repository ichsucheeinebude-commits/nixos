# modules/10-network

Auto-generated pure NixOS modules. **No personal data, no magic numbers.**

See individual module files for options. All values have safe defaults.

## New from MetaBibliothek

| Module | Description |
|--------|-------------|
| `20-adguardhome.nix` | AdGuard Home DNS filter with DoH, DNSSEC, blocklists |
| `21-tailscale.nix` | Tailscale VPN with auto-connect and SOPS integration |
| `22-cloudflared-tunnel.nix` | Cloudflare Tunnel for zero-port-forwarding ingress |
| `23-landing-zone-ui.nix` | Static landing page with rescue fallback |
| `24-dns-map.nix` | Central DNS subdomain mapping for all services |
